@model IEnumerable<Project_Dotnet.Models.Facilitie>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FacilitieName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FacilitieDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FacilitiePrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FacilitieName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FacilitieDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FacilitiePrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FacilitieID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FacilitieID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FacilitieID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
